#!/usr/bin/env bash


# Check if an IP address is provided as an argument
if [ $# -eq 0 ]; then
    echo "Usage: $0 {IP_ADDRESS}"
    exit 1
fi

# Extract the first argument as the IP address
IP_ADDRESS=$1

# Ping the IP address 5 times
echo "Pinging $IP_ADDRESS..."
ping -c 5 $IP_ADDRESS
